// js/config.js

const AppConfig = {
    // IMPORTANT: Replace with your REAL AdMob IDs from your AdMob account
    admob: {
        android: {
            // This is a Google-provided test ID. Replace it before publishing.
            interstitial: 'ca-app-pub-3940256099942544/1033173712'
        }
    },
    
    // The email address for user feedback or support
    supportEmail: 'support@yourdomain.com', 
    
    // Set to 'production' before building the final version to disable console logs
    mode: 'development' 
};
